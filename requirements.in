# Direct runtime dependencies. Generate requirements.txt with pip-compile.
fastapi
uvicorn
# uvloop is a uvicorn acceleration dependency, but its upstream package does
# not support Windows. Keep the production lock portable for the Windows
# launcher build.
uvloop; sys_platform != "win32"
python-multipart
python-dotenv
httpx
# CalDAV pulls urllib3-future through niquests. requests 2.34 assumes the
# standard urllib3 module layout and fails when that package is installed.
requests<2.34
websockets
pydantic>=2.0
pydantic-settings>=2.0
SQLAlchemy
pypdf>=6.16.1
beautifulsoup4
charset-normalizer
numpy
chromadb
fastembed
youtube-transcript-api
markdown
icalendar
python-dateutil
caldav
cryptography>=50.0.0
# crawl4ai's current pyOpenSSL pin accepts cryptography<50; this floor is the
# resolver-required companion for the cryptography security floor above.
pyopenssl>=26.4.0
bcrypt
mcp
pyotp
qrcode[pil]
croniter
crawl4ai
python-magic
# Security floors for packages present in the runtime graph. Keep these as
# minimums so pip-compile can retain unrelated lock pins.
aiohttp>=3.14.3
h2>=4.4.1
nltk>=3.10.3
soupsieve>=2.9
