[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
  "e2e: real-browser tests that start an isolated local application",
]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E9", "F811", "F821", "E722"]
